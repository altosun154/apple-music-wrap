// MusicKit developer token configuration.
//
// Generate a token with `scripts/generate-token.js` once you have an Apple
// Developer Program membership and a MusicKit private key, then paste it
// below. See README.md for the full walkthrough.
//
// Leave this blank to be prompted for a token in the browser instead (it's
// then kept only in your browser's localStorage, never sent anywhere but
// Apple's API).
window.MUSICKIT_DEVELOPER_TOKEN = "";

window.MUSICKIT_APP_NAME = "Apple Music Wrap";
window.MUSICKIT_APP_BUILD = "1.0.0";
